(function() {
    'use strict';

    angular
        .module('javagapApp')
        .controller('CsController', CsController);

    CsController.$inject = ['Principal', 'Auth', 'JhiLanguageService', '$translate'];

    function CsController (Principal, Auth, JhiLanguageService, $translate) {
        
       
    }
})();