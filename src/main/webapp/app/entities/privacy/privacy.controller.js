(function() {
    'use strict';

    angular
        .module('javagapApp')
        .controller('PrivacyController', PrivacyController);

    PrivacyController.$inject = ['Principal', 'Auth', 'JhiLanguageService', '$translate'];

    function PrivacyController (Principal, Auth, JhiLanguageService, $translate) {
        
       
    }
})();